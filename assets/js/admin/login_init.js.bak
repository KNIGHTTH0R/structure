var Login = function() {
	return {
		init: function() {
			var login_form = $( '#login-form' );
			
			login_form.on( 'submit', function( e ) {
				e.preventDefault();
				
				$.post( 'login/authenticate_admin', function( data ) {
					alert( data );
				});
			});
		}
	};
}();