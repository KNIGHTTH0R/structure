<?php
echo $page_access_level_id;
$CI =& get_instance();
	if( strtotime( '-1 minute' ) > $CI->session->userdata( 'last_activity' ) ) {
		$CI->session->set_userdata( [] );
		$access_levels = get_access_levels();
		print_r( $access_levels );
		if( $access_levels[$page_access_level_id] == 0 ) {
			echo 'true';
		} else {
			echo 'false';
		}
	} else {
		echo 'true';
	}
?>